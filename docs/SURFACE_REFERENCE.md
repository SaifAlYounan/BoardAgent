# BoardAgent surface reference

> AUTO-GENERATED inventory from the frozen Gate 2 registry and pinned administrative and operational amendments. Do not edit this file by hand.
> Registry digest: `d15e9d9f145589aaa513d560fed8fc7e234b1635a6c15ec5637d949b133a3f57`.

This is the human-readable inventory for BoardAgent's closed MCP, HTTP, CLI and event
surfaces. The generated manifest is the machine authority; live MCP `tools/list`,
`resources/list` and `prompts/list` expose the strict executable schemas and negotiated
profile. Purpose, authority and evidence rules below come from the same frozen source.

## Reading the inventory

- **R** is a non-mutating read. SQL authorization, board/object scope, ACL and recusal
  still apply on every request.
- **D** is a direct, nonbinding mutation. It is authorized, idempotent and audited, but
  does not use a fresh human-confirmation ceremony.
- **H** is a binding or security-sensitive mutation. The client must complete the exact
  prepare/present/confirm/act flow with recent authentication. Human approval is required;
  the server cannot establish how the client presented the request or obtained that approval.
- Unknown tools, fields, schema versions, capabilities and authority combinations fail
  closed. Safe errors do not disclose hidden object existence.

The normative decision and transaction details remain in
[the frozen surface matrix](../planning/gate2/gate2/SURFACE-AUTHORITY-EVENT-MATRIX.md).
Strict input contracts live in `lib/contracts/src/surface-inputs.ts` and related contract
modules; runtime exposure and safe results live in `artifacts/server/src/mcp-surface.ts`,
`artifacts/server/src/surface-read.ts`, and `artifacts/server/src/surface-service.ts`.

## MCP tools

### Context, onboarding and synchronized edge memory

- `whoami` — **R**. Authority: authenticated or enrollment-limited token Object: own identity only Evidence/result: safe identity/role/session view; `context_read`
- `list_my_boards` — **R**. Authority: `G:R` Object: active memberships only Evidence/result: entitlement-filtered list; no hidden counts
- `get_board` — **R**. Authority: `G:R` Object: active membership on board Evidence/result: `resource_fetch` for canonical board metadata
- `get_my_board_snapshot` — **R**. Authority: `G:R` Object: one active board membership Evidence/result: bounded metadata/resource references, cursor and entitlement generation
- `list_my_updates` — **R**. Authority: `G:R` Object: caller-bound signed cursor Evidence/result: deltas/tombstones only; cursor tamper is a safe denial
- `list_pending_actions` — **R**. Authority: `G:R` Object: own entitled objects only Evidence/result: one-call briefing; `revote_required` resolves only by accepted new-vote ballot, terminal/replacement vote, or eligibility loss; includes `minutes_resign_required` until current signature/terminal package
- `get_onboarding` — **R**. Authority: `O:R` Object: own role and current terms version Evidence/result: canonical terms, secretary support details and presentation/local-memory questions
- `get_onboarding_status` — **R**. Authority: `O:R` Object: own attestation only Evidence/result: current/stale/required state; no other member status
- `prepare_onboarding_attestation` — **H**. Authority: `O:R`; browser recent-auth completion Object: own current exact terms Evidence/result: `onboarding_stage_created`, `onboarding_attested`; no comprehension claim

### Boards, charter profile and rulesets

- `create_board` — **H**. Authority: admin + `S:A` Object: organization singleton Evidence/result: `board_created`; creates no implicit seats
- `update_board` — **H**. Authority: admin + `S:A` Object: nonterminal board Evidence/result: immutable settings version; `board_amended`
- `archive_board` — **H**. Authority: admin + `S:A` Object: no open governance action Evidence/result: visibility-only terminal transition; `board_archived`
- `get_board_governance_profile` — **R**. Authority: `G:R` Object: board membership Evidence/result: active version/hash and cited source clauses
- `list_approval_rule_templates` — **R**. Authority: `G:R` Object: board membership Evidence/result: only active-profile permitted templates
- `configure_board_governance` — **H**. Authority: admin + `S:A` Object: cited machine-readable charter sources Evidence/result: immutable activated profile; `governance_profile_activated`
- `list_matter_types` — **R**. Authority: `G:R` Object: board membership Evidence/result: active ruleset types/citations
- `evaluate_matter` — **D**. Authority: secretary/admin + `S:A` Object: entitled board and strict typed facts Evidence/result: persisted reproducible evaluation; `matter_evaluated`
- `get_ruleset` — **R**. Authority: `G:R` Object: board membership Evidence/result: exact version/canonical hash/citations
- `list_ruleset_versions` — **R**. Authority: `G:R` Object: board membership Evidence/result: immutable visible version history
- `validate_ruleset_draft` — **R**. Authority: admin/secretary + `S:A` Object: owned draft Evidence/result: deterministic validation only; no activation
- `manage_ruleset` — **H**. Authority: admin + `S:A` Object: exact version/citations and no ambiguity Evidence/result: `ruleset_amended`; activation/override package is confirmed

### Canonical documents and circulation

- `list_documents` — **R**. Authority: `D:R` Object: SQL entitlement predicate before count/page Evidence/result: no excluded existence, count or cursor leak
- `read_document` — **R**. Authority: `D:R` Object: exact document/version ACL and no exclusion Evidence/result: exact canonical bytes plus two-phase `resource_fetch`
- `search_documents` — **R**. Authority: `D:R` Object: visibility predicate before rank/snippet/count Evidence/result: deterministic PostgreSQL FTS; no server embeddings
- `get_document_hash` — **R**. Authority: `D:R` Object: exact version access Evidence/result: SHA-256/media/schema/length only
- `list_document_versions` — **R**. Authority: `D:R` Object: document access Evidence/result: immutable entitled versions only
- `get_document_validation_status` — **R**. Authority: contributor or `D:R` Object: own contribution or entitled document Evidence/result: loud accepted/rejected reason and remediation
- `create_document_version` — **D**. Authority: management/secretary + `D:C` Object: board contribution rights; strict MD/TXT/JSON only Evidence/result: reject unsupported bytes before durable content; `document_version_created`
- `circulate_document` — **H**. Authority: secretary + `S:A` Object: accepted immutable version and recipient ACL Evidence/result: `document_circulated` plus one `notice_delivered` per recipient
- `manage_document_access` — **H**. Authority: secretary/admin + `S:A` Object: board-bound grants; deny-wins exclusions Evidence/result: `document_access_changed` and removal tombstones
- `manage_recusal` — **H**. Authority: secretary/admin + `S:A` Object: named board/person/object or live vote exclusion Evidence/result: `recusal_changed`; invalidations/recalculation under aggregate lock
- `archive_document` — **H**. Authority: secretary/admin + `S:A` Object: no rewrite/delete Evidence/result: `document_archived`; normal listings hide it
- `soft_delete_document` — **H**. Authority: admin + `S:A` Object: snapshot first; no physical purge Evidence/result: `document_soft_deleted`, permanent tombstone and feed removals

### Management-to-secretariat submissions

- `submit_document_to_secretariat` — **D**. Authority: management + `D:C` Object: own board; accepted document versions only Evidence/result: immutable submission version/thread; `management_submission_created`
- `list_management_submissions` — **R**. Authority: management/secretary + `G:R` Object: own submissions or secretary board queue Evidence/result: entitlement-filtered queue
- `get_management_submission` — **R**. Authority: management/secretary + `G:R` Object: exact thread access Evidence/result: versions, replies and disposition lineage
- `request_management_revision` — **D**. Authority: secretary + `S:A` Object: submitted thread Evidence/result: immutable request; `management_revision_requested`
- `reply_to_management_revision` — **D**. Authority: submitting management + `D:C` Object: own thread Evidence/result: immutable reply; `management_revision_replied`
- `resubmit_management_materials` — **D**. Authority: submitting management + `D:C` Object: new immutable accepted document/submission version Evidence/result: `management_submission_version_created`; linked vote enters pending-source state
- `approve_management_submission` — **D**. Authority: secretary + `S:A` Object: submitted version Evidence/result: creates confirmable draft only; `management_submission_approved_to_draft`
- `reject_management_submission` — **D**. Authority: secretary + `S:A` Object: submitted version; reason required Evidence/result: `management_submission_rejected`; no deletion

### Permanent questions to management

- `ask_management` — **D**. Authority: voting member **or observer** + `MQ` Object: entitled board, ACL-safe citations, responsible owner and due time Evidence/result: immutable question; `management_question_asked`; management pending action
- `list_management_questions` — **R**. Authority: `G:R` Object: visible threads only; role/ACL/recusal filter before count Evidence/result: pending/overdue/answered state without hidden totals
- `get_management_question` — **R**. Authority: `G:R` Object: visible exact thread Evidence/result: immutable turns/citations/delivery lineage
- `answer_management_question` — **D**. Authority: assigned management + `MQ` Object: pending/overdue visible thread; nonblank answer Evidence/result: immutable answer; `management_question_answered`; cannot answer by status flip; a new turn beyond an open linked package cutoff enters source-update pending
- `follow_up_management_question` — **D**. Authority: entitled voting member **or observer** + `MQ` Object: visible thread Evidence/result: immutable follow-up; `management_question_followed_up`; reopens management action and, only when thread is linked to an open package, source-update pending

### Meetings, agendas and attendance

- `list_meetings` — **R**. Authority: `G:R` Object: board membership and ACL Evidence/result: entitled meetings only
- `get_agenda` — **R**. Authority: `G:R` Object: meeting/agenda/document access Evidence/result: exact immutable agenda version/resources
- `rsvp` — **D**. Authority: nonobserver board seat + `MT:A` Object: called, nonterminal meeting and own response Evidence/result: append/update with history; `meeting_rsvp_recorded`
- `get_attendance` — **R**. Authority: `G:R` Object: board membership Evidence/result: recorded evidence; never inferred from RSVP
- `create_meeting` — **H**. Authority: secretary + `S:A` Object: final canonical package from guided draft Evidence/result: `meeting_called` plus one recipient notice
- `amend_meeting` — **H**. Authority: secretary + `S:A` Object: called/nonterminal; immutable replacement version Evidence/result: `meeting_amended`; stale stages invalidated and recipients re-noticed
- `record_attendance` — **D**. Authority: secretary + `S:A` Object: called/completed meeting and listed participant Evidence/result: `meeting_attendance_recorded`; correction uses a new record
- `correct_attendance` — **H**. Authority: secretary + `S:A` Object: exact prior record and reason Evidence/result: linked correction; `meeting_attendance_corrected`
- `cancel_meeting` — **H**. Authority: secretary + `S:A` Object: nonterminal meeting Evidence/result: `meeting_cancelled`; terminal and re-noticed
- `complete_meeting` — **H**. Authority: secretary + `S:A` Object: called meeting Evidence/result: `meeting_completed`; terminal evidence snapshot

### Machine-readable transcript annexes

- `list_meeting_transcripts` — **R**. Authority: `G:R` Object: meeting access Evidence/result: versions and loud verification state
- `get_meeting_transcript` — **R**. Authority: `G:R` Object: meeting/transcript ACL Evidence/result: canonical MD/strict-JSON turns; `resource_fetch`
- `create_meeting_transcript_version` — **D**. Authority: secretary + `S:A` Object: meeting; machine-readable text only Evidence/result: immutable `agent_prepared_unverified` annex; `transcript_version_created`
- `verify_meeting_transcript` — **H**. Authority: secretary + `S:A` Object: exact annex hash/version Evidence/result: `transcript_secretary_verified`; no claim about original audio
- `link_meeting_qna` — **H**. Authority: secretary + `S:A` Object: exact transcript turns and D2-062 threads Evidence/result: immutable link; `transcript_qna_linked`; unanswered turns create management actions
- `challenge_transcript_turn` — **D**. Authority: entitled nonobserver participant + `S:M` Object: exact visible turn Evidence/result: immutable challenge; `transcript_turn_challenged`
- `resolve_transcript_challenge` — **H**. Authority: secretary + `S:A` Object: challenge plus corrected new version Evidence/result: `transcript_challenge_resolved`; never rewrites old turn

### Votes, proxies and certificates

- `list_votes` — **R**. Authority: `G:R` Object: board membership and tally-visibility policy Evidence/result: no hidden vote/statement/interim-count leak
- `get_vote` — **R**. Authority: `G:R` Object: exact vote access Evidence/result: canonical package/hash/rule and authorized tally state
- `get_vote_lineage` — **R**. Authority: `G:R` Object: access to linked votes Evidence/result: draft/open/superseded/replacement IDs and hashes
- `stage_ballot` — **H**. Authority: eligible voting seat + `V:A` Object: open, unrecused, current package; own or valid proxy principal Evidence/result: MRTR confirmation; `stage_created`, `elicitation_sent`, `consent_recorded`, `ballot_cast`
- `grant_proxy` — **H**. Authority: eligible principal + `P:M` Object: open vote, permitted holder, no chain/cycle Evidence/result: `proxy_granted`; no pre-consent grant row
- `revoke_proxy` — **H**. Authority: principal + `P:M` Object: own active grant and frozen policy Evidence/result: `proxy_revoked`; effects explicit/audited
- `get_proxy_status` — **R**. Authority: principal/holder/secretary + `G:R` Object: exact vote and relationship Evidence/result: safe attribution/status only
- `get_vote_certificate` — **R**. Authority: `G:R` or `A:R` Object: board-authorized vote Evidence/result: persisted recomputed bundle and signature
- `verify_certificate` — **R**. Authority: authenticated Object: supplied/visible certificate Evidence/result: persisted-data recomputation; generic reason class
- `create_vote` — **H**. Authority: secretary + `S:A` Object: completed guided package/profile-permitted rule Evidence/result: `vote_opened`, package/electorate freeze, per-member notices
- `amend_resolution_text` — **H**. Authority: secretary + `S:A` Object: draft creates version; open vote invokes replacement invariant Evidence/result: `resolution_amended`; when open also `vote_superseded`/new `vote_opened`
- `replace_open_vote` — **H**. Authority: secretary + `S:A` Object: open or source-update-pending vote and exact replacement package Evidence/result: atomically disposes every old act, creates new open vote/electorate/package, `vote_opened`, per-newly-eligible notice/`notice_delivered`, informational `vote_replaced`, and actionable `revote_required` only for still-eligible prior principals
- `exclude_pending_vote_source` — **H**. Authority: secretary + `S:A` Object: exact pending source with nonblank reason Evidence/result: `vote_source_excluded`; original package remains byte-identical
- `extend_vote_deadline` — **H**. Authority: secretary + `S:A` Object: draft version or open-vote replacement only Evidence/result: no in-place open extension; replacement events/notices
- `close_vote` — **H**. Authority: secretary + `S:A`; only for `secretariat_confirmed` close mode Object: exact open vote, healthy clock, one tally kernel Evidence/result: `vote_closing`, signed certificate, then `vote_closed` atomically recoverable; automatic close is an internal typed worker operation, never this MCP tool
- `cancel_vote` — **H**. Authority: secretary + `S:A` Object: open/draft, exact reason Evidence/result: `vote_cancelled`; terminal, acts retained non-outcome-bearing

### Minutes and signatures

- `get_minutes` — **R**. Authority: `G:R` Object: meeting/minutes access Evidence/result: exact version/hash/transcript annex refs
- `list_minutes_versions` — **R**. Authority: `G:R` Object: minutes access Evidence/result: immutable visible versions
- `get_minutes_lineage` — **R**. Authority: `G:R` Object: access to original/correction aggregates Evidence/result: immutable package/correction IDs, versions, hashes and terminal states
- `create_minutes_version` — **D**. Authority: secretary + `S:A` Object: unpublished draft only; accepted canonical text/transcript refs Evidence/result: `minutes_version_created`; no publication; published/finalized input rejects
- `publish_minutes` — **H**. Authority: secretary + `S:A` Object: exact unpublished draft version and proposed signer set Evidence/result: enters `published_review`; `minutes_published`, per-recipient review notices
- `list_minutes_review_items` — **R**. Authority: entitled participant + `G:R` Object: exact visible minutes package Evidence/result: immutable comments/redlines/withdrawals/dispositions only
- `comment_minutes` — **D**. Authority: entitled voting member **or observer** + `M:A` Object: current `published_review` package; strict `boardagent.minutes-comment.v1`, exact base hash and anchor Evidence/result: immutable comment; `minutes_commented`
- `withdraw_minutes_comment` — **D**. Authority: entitled comment author, including observer + `M:A` Object: own pending comment on current `published_review` package Evidence/result: immutable `minutes_review_withdrawn`; text remains permanent
- `propose_minutes_redline` — **D**. Authority: entitled voting member **or observer** + `M:A` Object: current `published_review` package; strict `boardagent.minutes-redline.v1`; exact base/version/line anchor/old-text hash; no attachment Evidence/result: `minutes_redline_proposed`; never applies content itself
- `resolve_minutes_review_item` — **H**. Authority: secretary + `S:A` Object: exact pending nonwithdrawn item and reason; redline acceptance supplies canonical replacement Evidence/result: `minutes_review_dispositioned`; accepted redline creates version/diff and returns the new package to review
- `correct_minutes_package` — **H**. Authority: secretary + `S:A` Object: published/signature-ready but nonfinalized current package Evidence/result: new immutable version; `minutes_package_corrected`; supersedes stale draft actions/stages/signatures, returns to review and emits current re-sign notices
- `prepare_minutes_for_signature` — **H**. Authority: secretary + `S:A` Object: all review items dispositioned; exact action-items-or-none declaration and signer set Evidence/result: freezes package; `minutes_signature_package_issued`
- `stage_minutes_signature` — **H**. Authority: configured entitled voting member **or observer** + `M:A` Object: exact current minutes/package hash Evidence/result: standard MRTR; `minutes_signed`; observer attestation is non-voting
- `finalize_minutes` — **H**. Authority: secretary + `S:A` Object: required current-package signatures complete Evidence/result: `minutes_action_items_activated` plus `minutes_finalized`; activation manifest is bound; terminal
- `create_minutes_correction_cycle` — **H**. Authority: secretary + `S:A` Object: exact finalized minutes, correction reason and new canonical package Evidence/result: original stays terminal; new linked aggregate enters `published_review`; `minutes_correction_cycle_created`
- `cancel_minutes` — **H**. Authority: secretary + `S:A` Object: nonfinalized package and reason Evidence/result: `minutes_cancelled`; immutable terminal history

### Tasks, proposals and secretariat messages

- `list_my_tasks` — **R**. Authority: `G:R` Object: own assigned tasks Evidence/result: own evidence/status only
- `list_action_items` — **R**. Authority: `G:R` Object: board/minutes ACL; observers read entitled items Evidence/result: status/owner/due/source/evidence refs without hidden counts
- `get_task` — **R**. Authority: `G:R` Object: assignee, entitled board participant or secretary/creator Evidence/result: exact task/evidence lineage subject to source ACL
- `get_action_item` — **R**. Authority: `G:R` Object: exact entitled minutes-linked task Evidence/result: source minutes/version/hash, owner, due, evidence/review/closure lineage
- `log_minutes_action_items` — **H**. Authority: secretary + `S:A` Object: exact review-resolved minutes version; structured nonempty list Evidence/result: immutable **draft** tasks/manifests; `minutes_action_items_declared`; no extraction/owner notice before finalization
- `declare_no_minutes_action_items` — **H**. Authority: secretary + `S:A` Object: exact review-resolved minutes version Evidence/result: explicit hashed declaration; `minutes_action_items_declared`
- `create_task` — **H**. Authority: secretary + `S:A` Object: active board/member and canonical assignment Evidence/result: `task_created`; assignee pending action
- `start_task` — **D**. Authority: assignee + `T:A` Object: own open task Evidence/result: `task_started`
- `submit_task_evidence` — **D**. Authority: assignee + `T:A` Object: own nonterminal task; canonical evidence refs only Evidence/result: immutable evidence; `task_evidence_submitted`
- `review_task_evidence` — **H**. Authority: secretary + `S:A` Object: pending exact evidence; reason required Evidence/result: `task_evidence_reviewed`; rejection preserves evidence and returns item to open
- `complete_task` — **H**. Authority: secretary + `S:A` Object: exact minutes/task basis and at least one accepted evidence hash Evidence/result: `task_completed`; closure hash binds all accepted evidence; terminal
- `create_task_correction_cycle` — **H**. Authority: secretary + `S:A` Object: exact completed task, reason and new canonical task/evidence requirements Evidence/result: completed original remains terminal; linked open cycle; `task_correction_cycle_created`
- `cancel_task` — **H**. Authority: secretary + `S:A` Object: nonterminal and reason Evidence/result: `task_cancelled`; terminal
- `propose_action` — **D**. Authority: voting member/management + `MP` Object: active board and strict proposal schema Evidence/result: `proposal_submitted`; never executes
- `withdraw_proposal` — **D**. Authority: proposer + `MP` Object: own pending proposal Evidence/result: `proposal_withdrawn`; payload retained
- `list_proposals` — **R**. Authority: secretary + `S:A` Object: board queue Evidence/result: entitled proposals only
- `approve_proposal` — **D**. Authority: secretary + `S:A` Object: pending proposal Evidence/result: `proposal_approved_to_draft`; creates draft only
- `reject_proposal` — **D**. Authority: secretary + `S:A` Object: pending proposal and reason Evidence/result: `proposal_rejected`
- `ask_secretariat` — **D**. Authority: nonobserver member/management + `S:M` Object: active board Evidence/result: immutable request; `secretariat_request_created`
- `list_secretariat_requests` — **R**. Authority: requester or secretary + `G:R` Object: own request or board queue Evidence/result: no cross-board visibility
- `reply_secretariat_request` — **D**. Authority: secretary + `S:A` Object: board request Evidence/result: immutable response; `secretariat_request_replied`
- `close_secretariat_request` — **D**. Authority: requester or secretary + `S:M` Object: answered request only Evidence/result: `secretariat_request_closed`; no deletion

### Members, enrollment, sessions and clients

- `list_members` — **R**. Authority: secretary/admin + `S:A` Object: organization/board administrative view Evidence/result: minimal identities/roles; no credential material
- `get_member` — **R**. Authority: own identity or secretary/admin + `S:A` Object: exact authorized member Evidence/result: accountable principal shown for AI observer
- `manage_member` — **H**. Authority: Current human admin + `S:A`, OR current human secretary with live exact-board member-administration delegation + `S:A` and strictly allowed operation/target. Object: Admin behavior preserved. Delegate: non-null granted board only, ordinary human voting director, no self/privileged/observer/management/AI target, no secretary/admin/other-delegate changes or onward delegation. Evidence/result: Existing exact H and history/revocation, plus immutable board-readable appointment citations and bounded reason for delegated changes.
- `issue_enrollment` — **H**. Authority: secretary/admin + `S:A` Object: precreated seat; no active invitation Evidence/result: one-time reference/QR payload only once; `enrollment_issued`
- `list_enrollments` — **R**. Authority: secretary/admin + `S:A` Object: organization Evidence/result: status/expiry only, no bearer material
- `revoke_enrollment` — **H**. Authority: secretary/admin + `S:A` Object: exact live invitation Evidence/result: `enrollment_revoked`
- `confirm_enrollment_activation` — **H**. Authority: secretary/admin + `S:A` Object: pending seat, exact 10-minute human code and identity-proof method Evidence/result: `member_activated`; never approves link possession alone
- `initiate_identity_recovery` — **H**. Authority: secretary/admin + `S:A` Object: verified person/seat and reason Evidence/result: old sessions/credentials handled explicitly; `identity_recovery_started`
- `list_my_sessions` — **R**. Authority: authenticated Object: own sessions only Evidence/result: client/time/status; token values absent
- `revoke_my_session` — **H**. Authority: authenticated recent auth Object: own named refresh family/session Evidence/result: `session_revoked`
- `list_oauth_clients` — **R**. Authority: admin + `S:A` Object: deployment Evidence/result: safe client metadata/status only
- `block_oauth_client` — **H**. Authority: admin + `S:A` Object: exact server-issued client ID Evidence/result: grants/families revoked; `oauth_client_blocked`
- `unblock_oauth_client` — **H**. Authority: admin + `S:A` Object: exact blocked client Evidence/result: `oauth_client_unblocked`; no implicit grants
- `link_external_identity` — **H**. Authority: admin + `S:A` and browser subject proof Object: existing invited member and exact issuer/subject Evidence/result: `external_identity_linked`
- `unlink_external_identity` — **H**. Authority: admin + `S:A` Object: preserve at least one recovery path Evidence/result: `external_identity_unlinked` and sessions revoked

### Audit, export, webhooks and permanent-record policy

- `export_audit_chain` — **H**. Authority: `A:R` + recent auth Object: authorized organization/board scope Evidence/result: async frozen request; `export_requested`
- `verify_audit_chain` — **R**. Authority: `A:R` Object: authorized chain/chunk or supplied export Evidence/result: recompute and report first break
- `export_system_data` — **H**. Authority: admin + `S:A` + recent auth Object: explicit frozen scope Evidence/result: excludes all secret material; `export_requested`
- `get_export_status` — **R**. Authority: requesting principal + original scope Object: unguessable request ID Evidence/result: state/manifest hash only
- `read_export_chunk` — **R**. Authority: requesting principal + original scope + recent auth Object: ready artifact and bounded chunk Evidence/result: exact chunk hash plus `resource_fetch`
- `cancel_export` — **H**. Authority: requesting principal + original authority Object: queued/running request Evidence/result: `export_cancelled`; partial artifacts quarantined
- `delete_export_artifact` — **H**. Authority: requesting principal + original authority Object: ready artifact Evidence/result: artifact destroyed, permanent receipt/hash; `export_artifact_deleted`
- `get_retention_policy` — **R**. Authority: `A:R` or admin + `S:A` Object: deployment Evidence/result: reports indefinite record retention and expiring ephemera classes
- `list_my_webhooks` — **R**. Authority: `N:M` Object: own member endpoints Evidence/result: URL redacted; secret absent
- `configure_webhook` — **H**. Authority: `N:M` + recent auth Object: own HTTPS public endpoint; SSRF checks Evidence/result: one-time secret display; `webhook_configured`
- `rotate_webhook_secret` — **H**. Authority: `N:M` + recent auth Object: own endpoint Evidence/result: old secret invalidated; new secret never idempotently replayed
- `disable_webhook` — **H**. Authority: `N:M` Object: own endpoint Evidence/result: `webhook_disabled`
- `test_webhook` — **D**. Authority: `N:M` Object: own verified endpoint Evidence/result: contentless test wake-up; `webhook_tested`

### Wizard draft lifecycle

- `list_my_drafts` — **R**. Authority: secretary/management/member according to draft type Object: own nonexpired drafts Evidence/result: safe summaries only
- `resume_draft` — **R**. Authority: original creator plus current authority Object: signed draft/client binding; client portability policy Evidence/result: exact current step/package; no mutation
- `cancel_draft` — **D**. Authority: original creator Object: own active draft Evidence/result: `draft_cancelled`; immutable answers retained

### Administrative succession and delegation

- `manage_company_admin` — **H**. Authority: Current human identity + `S:A`; admin for propose/transfer/revoke, named recipient for accept/decline, proposer/current admin for cancel; exact recent-auth H. Observer identities excluded. Object: Same organization, exact target/proposal/assignment and versions; pending proposals confer no authority; last active human admin preserved. Evidence/result: Exact consent, immutable role/proposal evidence and event; elevation/revocation invalidates affected connections.
- `manage_member_admin_delegation` — **H**. Authority: Current human admin + `S:A`; exact recent-auth H. Object: Exactly one active board and active human secretary; grant expiry at most 90 days; no onward delegation. Evidence/result: Exact consent, reason, 1–8 board-readable immutable appointment citations, grant/revoke event and connection invalidation.
- `list_administrative_access` — **R**. Authority: Current human identity + `S:A`; own mode only, or current admin for organization mode. Object: Own rights/pending offers only by default; exact organization/optional board filter and signed actor/generation-bound pagination; no private cross-board inventory. Evidence/result: Bounded current authority and pending proposal references; at most 100 entries; no tokens or credential material.

### Identity and onboarding

- `publish_secretary_support` — **H**. Authority: current board secretary + `S:A`; company admin + `S:A` only for its first support version Object: One active same-organization board; latest expected support version; no org-level default; current human eligibility and no board recusal. First version may be initialized by company admin without a seat; later updates require current board secretary authority. Evidence/result: Exact canonical contact version, protected confirmation, secretary_support_published and immutable preceding versions.
- `publish_onboarding_terms` — **H**. Authority: company admin + `S:A` Object: Own organization and one supported seat role; latest expected terms version; current human administrative eligibility; grants no board reads. Evidence/result: Exact canonical terms version, protected confirmation, onboarding_terms_published; fresh own attestation required, no automatic acceptance.
- `reissue_activation` — **H**. Authority: company admin + `S:A`; current board secretary + `S:A` for a pending seat on their own board. The first setup administrator's own pending activation is served only by the operator CLI mode `bootstrap reissue-first-activation`. Object: Exactly one same-organization member in `pending_activation` with an active registered passkey whose latest activation challenge is expired or exhausted and never consumed; not the caller; no live (issued, unexpired, unexhausted) challenge; the consumed invitation stays consumed. Issues a one-use ten-minute restart handoff; the fresh ten-minute code is minted only after a user-verified assertion with the already-registered passkey. Evidence/result: Protected confirmation naming the member, the stale challenge and the proofing method; `activation_restart_issued` linking the revoked challenge and the handoff; `activation_restart_completed` linking the handoff and the fresh challenge; the later `confirm_enrollment_activation` binds that fresh challenge. Append-only history.

## Resource templates

Resources are stable `board://` representations behind the same authenticated MCP
endpoint. Fetch authorization is recomputed; prepared/completed/interrupted evidence is
honest about server transport and never claims human reading.

- `board://{boardId}` — representations: canonical JSON metadata; entitlement root: board membership.
- `board://{boardId}/governance-profile/{version}` — representations: canonical JSON + cited clauses; entitlement root: board membership.
- `board://{boardId}/rulesets/{version}` — representations: canonical JSON; entitlement root: board membership.
- `board://{boardId}/documents/{documentId}/versions/{version}` — representations: exact UTF-8 MD/TXT or canonical typed JSON; entitlement root: document ACL minus exclusion.
- `board://{boardId}/submissions/{submissionId}/versions/{version}` — representations: canonical JSON/resource refs; entitlement root: management thread policy.
- `board://{boardId}/questions/{questionId}` — representations: canonical immutable-turn JSON; entitlement root: question ACL minus exclusion.
- `board://{boardId}/meetings/{meetingId}/agendas/{version}` — representations: canonical JSON/resource refs; entitlement root: meeting/document policy.
- `board://{boardId}/meetings/{meetingId}/transcripts/{version}` — representations: canonical MD or typed turn JSON; entitlement root: meeting/transcript policy.
- `board://{boardId}/minutes/{minutesId}/versions/{version}` — representations: exact canonical text/package JSON; entitlement root: minutes policy.
- `board://{boardId}/minutes/{minutesId}/review/{itemId}` — representations: strict comment/redline/disposition JSON; entitlement root: minutes participant policy.
- `board://{boardId}/action-items/{taskId}` — representations: canonical task/source/evidence/closure JSON; entitlement root: source minutes/task ACL.
- `board://{boardId}/votes/{voteId}` — representations: canonical vote metadata; entitlement root: vote policy.
- `board://{boardId}/votes/{voteId}/packages/{version}` — representations: exact decision-package JSON + hashes; entitlement root: vote/document/Q&A intersection.
- `board://{boardId}/votes/{voteId}/certificates/{certificateId}` — representations: signed bundle; entitlement root: authenticated certificate policy.
- `board://{boardId}/tasks/{taskId}` — representations: canonical task/evidence refs; entitlement root: assignee/secretariat policy.
- `export://{exportId}/chunks/{chunkNo}` — representations: encrypted/bounded exact bytes; entitlement root: original requester/scope/recent auth.

## Prompts

- `onboard-boardagent` — Explain agent-only responsibilities, ask desired presentation/local-memory form, surface secretary support, then prepare exact attestation.
- `set-vote` — Start/resume the cited-rule vote wizard ending in `create_vote`.
- `call-meeting` — Start/resume the meeting wizard ending in `create_meeting`.
- `circulate-document` — Validate/select canonical material and recipients ending in `circulate_document`.
- `record-minutes` — Build minutes and optional machine-readable transcript annex, require structured action-items-or-none, run machine-readable review/redline disposition, then issue the signature package.
- `configure-ruleset` — Build/validate cited typed rules ending in `manage_ruleset`.
- `ask-management` — Collect a scoped question, citations, responsible management owner and due time ending in `ask_management`.

## Browser and HTTP authority families

- `POST /mcp` — exact sessionless MCP `2026-07-28`; bounded `2025-11-25` read/resource/prompt profile on the same canonical resource
- `/.well-known/oauth-protected-resource/mcp` — public path-aware RFC 9728 metadata whose `resource` is exactly normalized `https://host/mcp`; every 401 challenge points here
- authorization-server metadata and JWKS — public protocol metadata/current+retired verification keys
- CIMD client-ID resolution and `/oauth/register` — outbound CIMD resolution is SSRF-hardened; DCR fallback is open, rate-limited and audited; protocol client ID and internal UUID remain distinct
- `/oauth/authorize`, `/oauth/token`, `/oauth/revoke` — code+S256 PKCE, exact resource, rotated refresh; no query bearer token
- `/auth/passkey/*`, `/auth/totp/*`, `/auth/oidc/*` — minimal hardened interactions, exact RP/origin/state/nonce/session binding
- `/enroll/{opaque}` — one-use invite redemption to pending activation; link alone grants nothing
- `/onboarding/{opaque}` — exact current terms and secure attestation after identity activation
- `POST /verify/certificate` — rate-limited unguessable bundle/ID, persisted-data recomputation, generic verdict
- `/health/live`, `/health/ready` — no secret/config/data disclosure; readiness includes schema/key/storage invariants
- `/metrics` — disabled or separately operator-authenticated; no protected identifiers

## Operator CLI authority

- `bootstrap` — sole preidentity exception; local operator, one-use singleton transaction, creates organization/first admin-secretary/invite and audit event
- `migrate` / `migration-check` — separate DB migrator role; checksum/advisory-lock/forward-only production policy
- `export` — online authenticated principal invoking the same export service and `H` policy; never direct DB dump
- `verify-chain` — offline read-only verifier over exported bytes
- `verify-certificate` — offline read-only stateless verifier over bundle/public keys
- `backup` — separate operator role, consistent DB/WAL manifest and encrypted destination; no governance mutation
- `restore-check` — isolated read-only full integrity validation; never repairs evidence
- `worker` — service principal limited to typed jobs; cannot mint tokens or bypass board/object IDs
- `audit-recovery` — Local migrator/operator only; inspect and prepare are read-only, apply binds the exact instance, request digest and observed audit state. Existing implemented recovery preserves original history and permanent missed-deadline findings; no governance or human-consent authority.
- `key-lifecycle` — Local migrator/operator only; prepare validates exact key identities, dependencies and protected staged files, apply commits the exact maintenance transition and immutable audit receipt, inspect resolves interruption/retry. No agent or company-administrator authority. Registry reservation only until the supported command, five-purpose transitions, restart and restore tests are implemented and qualified.

## Event registry

Events have strict versioned payloads and are appended inside the transaction that makes
their claim true. An event name is evidence of its defined server-side fact only.

### Protocol, identity and policy

`context_read`, `client_registered`, `client_registration_rejected`, `oauth_client_blocked`, `oauth_client_unblocked`, `token_issued`, `token_refreshed`, `token_reuse_detected`, `session_revoked`, `enrollment_issued`, `enrollment_redeemed`, `enrollment_revoked`, `member_activated`, `identity_recovery_started`, `external_identity_linked`, `external_identity_unlinked`, `member_changed`, `onboarding_stage_created`, `onboarding_attested`, `onboarding_stale`, `authorization_denied`, `rate_limited`

### Record delivery, documents and communications

`resource_fetch`, `notice_delivered`, `document_version_created`, `document_circulated`, `document_access_changed`, `document_archived`, `document_soft_deleted`, `recusal_changed`, `management_submission_created`, `management_revision_requested`, `management_revision_replied`, `management_submission_version_created`, `management_submission_approved_to_draft`, `management_submission_rejected`, `management_question_asked`, `management_question_answered`, `management_question_followed_up`, `secretariat_request_created`, `secretariat_request_replied`, `secretariat_request_closed`

### Governance, consent and evidence

`board_created`, `board_amended`, `board_archived`, `governance_profile_activated`, `matter_evaluated`, `ruleset_amended`, `rule_overridden`, `stage_created`, `stage_replaced`, `elicitation_sent`, `consent_recorded`, `consent_rejected`, `ballot_cast`, `ballot_superseded`, `proxy_granted`, `proxy_revoked`, `resolution_amended`, `vote_opened`, `vote_source_update_pending`, `vote_source_excluded`, `vote_closing`, `vote_closed`, `vote_cancelled`, `vote_superseded`, `vote_replaced`, `revote_required`, `certificate_issued`, `certificate_corrected`, `meeting_called`, `meeting_amended`, `meeting_rsvp_recorded`, `meeting_attendance_recorded`, `meeting_attendance_corrected`, `meeting_completed`, `meeting_cancelled`, `transcript_version_created`, `transcript_secretary_verified`, `transcript_qna_linked`, `transcript_turn_challenged`, `transcript_challenge_resolved`, `minutes_version_created`, `minutes_published`, `minutes_commented`, `minutes_review_withdrawn`, `minutes_redline_proposed`, `minutes_review_dispositioned`, `minutes_package_corrected`, `minutes_correction_cycle_created`, `minutes_action_items_declared`, `minutes_action_item_draft_superseded`, `minutes_action_items_activated`, `minutes_signature_package_issued`, `minutes_signed`, `minutes_signature_superseded`, `minutes_resign_required`, `minutes_finalized`, `minutes_cancelled`

### Workflow, export and operations

`task_created`, `task_started`, `task_evidence_submitted`, `task_evidence_reviewed`, `task_completed`, `task_correction_cycle_created`, `task_cancelled`, `proposal_submitted`, `proposal_withdrawn`, `proposal_approved_to_draft`, `proposal_rejected`, `draft_cancelled`, `draft_expired`, `export_requested`, `export_started`, `export_performed`, `export_failed`, `export_cancelled`, `export_artifact_deleted`, `webhook_configured`, `webhook_secret_rotated`, `webhook_disabled`, `webhook_tested`, `webhook_delivery_attempted`, `audit_checkpoint_signed`, `audit_verification_failed`, `migration_applied`, `backup_completed`, `restore_verified`

### Administrative succession and delegation

`company_admin_proposed`, `company_admin_proposal_cancelled`, `company_admin_proposal_declined`, `company_admin_granted`, `company_admin_revoked`, `company_admin_transferred`, `member_admin_delegation_granted`, `member_admin_delegation_revoked`

### Operator key maintenance

`key_lifecycle_changed`

### Identity and onboarding

`secretary_support_published`, `onboarding_terms_published`, `activation_restart_issued`, `activation_restart_completed`

## Discovery, inputs, outputs and retries

1. Negotiate the exact supported MCP profile and client capability set at the canonical
   `/mcp` resource. Modern calls are sessionless; the bounded legacy profile is read-only.
2. Discover the current tool/resource/prompt schema from the server. Do not reconstruct
   payloads from agent memory or this prose reference.
3. Resolve canonical IDs and versions through entitled reads. Generated text, a resource
   title or a local-memory summary is never an authority token.
4. For an H action, present the entire exact confirmation package to the human and submit
   only the returned stage/nonce with the same authenticated principal, client and origin.
5. Reuse an idempotency key only for the identical canonical request. Conflict, stale
   version, superseded stage and expired/replayed confirmation are terminal until refetch.
6. Retry only safe reads and explicitly retryable transport/job failures. On an ambiguous
   mutation result, refetch canonical state and audit evidence before deciding what happened.

Content, free-text, count, cursor and error outputs remain subject to the same deny-wins
authorization and recusal rules. Optional webhooks are contentless wake-ups; polling and
canonical refetch remain authoritative. BoardAgent records server handoff, exact consent and
governance state, not comprehension, independent judgment or legal effect.
